{
  "name": "compas-guitarra",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "check": "node --check src/game.js && node --check src/pitch.js && node scripts/test-pitch.mjs && node scripts/test-game.mjs",
    "dev": "python3 -m http.server 8000"
  },
  "engines": { "node": ">=22" }
}
